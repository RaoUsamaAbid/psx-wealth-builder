{
  "name": "psx-wealth-builder",
  "version": "0.1.0",
  "private": true,
  "description": "Long-term wealth creation platform for Pakistan Stock Exchange investors",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build -w @psx/shared -w @psx/engines -w @psx/market-data",
    "build:apps": "npm run build -w @psx/api -w @psx/web",
    "build": "npm run build:packages && npm run build:apps",
    "dev": "npm run dev:api & npm run dev:web",
    "dev:api": "npm run build:packages && npm run dev --workspace @psx/api",
    "dev:web": "npm run dev --workspace @psx/web",
    "seed": "npm run build:packages && npm run seed --workspace @psx/api",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "npm run build:packages && npm run typecheck --workspaces --if-present",
    "test": "npm run build:packages && npm run build -w @psx/api && vitest run",
    "test:watch": "vitest",
    "test:coverage": "npm run build:packages && npm run build -w @psx/api && vitest run --coverage",
    "prepare": "node scripts/prepare.mjs"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "@eslint/js": "^9.17.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "@testing-library/user-event": "^14.5.2",
    "@types/supertest": "^6.0.2",
    "@vitest/coverage-v8": "^2.1.8",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "husky": "^9.1.7",
    "jsdom": "^25.0.1",
    "mongodb-memory-server": "^10.1.4",
    "prettier": "^3.4.2",
    "supertest": "^7.0.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0",
    "vitest": "^2.1.8"
  }
}
